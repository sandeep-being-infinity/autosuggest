# autosuggest

Verifying my git set-up : )


Changes flow for pushing

**UI:**
1. add(plus button)
2. commit with message
3. push / sync

**commands : **
git add -A
git commit -m "some messsage"
git push